# IP 週報 App 使用說明

**網址**：https://harry13428.github.io/ip-report/
（跟網感訓練 App 完全分開，不同 repo、不同網址、不同資料）

## 這是什麼
剪輯師每週五回報「自己負責的 IP 這週的片況」的手機網頁 App。
每個人填自己的，所有人都看得到彼此的，主管打開「全部人」就是整週彙整。
**不用登入、不用鑰匙**：開網址、打名字就能用。

## 怎麼用（v3.0 起）
- 打開網址 → 出現名字清單 → **點自己的名字**就進去了（名單裡沒有你就打名字按「進去」）。換一台裝置開也是點一下名字，不用設定。
- 上面的日期列：‹ › 切前一天／後一天，點中間 📅 打開日曆選日期；日期下面有點＝那天有人填過。
- 「我的回報」：＋新增自己負責的 IP（加一次就好）→ 點 IP 展開 → 先填「這天的影片」→ 1～7 欄 → 最後選狀態 → 儲存並上傳。
- 「全部人」：每個人一張卡 → 底下列出他填的 IP（狀態燈＋片名）→ 點 IP 展開看那支片的狀況；紅字＝需要主管協助；上面可篩卡住／需協助／等回覆。
- ☰（左上角）：換人、打開 Google 試算表、複製這天全部人的回報文字、下載備份、重新下載。
- 舊的「設定／鑰匙／專用連結／測試連線」全部拿掉了。

## 資料放哪（後台，平常不用碰）
- App 本體：GitHub repo `harry13428/ip-report`（公開，只有網頁檔）
- 回報資料：KK 的 Google 試算表「IP週報」
  https://docs.google.com/spreadsheets/d/1rpuVje8QJrwARumPDnT6S7eyKlNMSe6Y14JBXWfY3us/edit
  分頁「回報」＝每人每週每 IP 一列；分頁「剪輯師」＝每人的 IP 清單
- 中間的程式：試算表的「擴充功能 → Apps Script」專案「IP週報」，正本在 `google試算表版/Code.gs`
  網頁應用程式網址（App 裡 API_URL）：
  https://script.google.com/macros/s/AKfycbycx6jdZdggGz4iVkDJ5ZkjoyT5n-7X2Vt0hbn86iwU1KgaFrb3RvZoS_XWhcDy3aN4/exec
- 舊的 GitHub 鑰匙版（v1.2）留在 `google試算表版/舊版_github鑰匙版_index_v1.2.html`，已不用；repo `ip-report-data` 與 GitHub 鑰匙「智慧財產週報」可以刪

## 更新 App（給 AI 或 KK）
正本在 `桌面/IP週報App/index.html`。改完：
```bash
cd ~/Desktop/IP週報App && git add -A && git commit -m "更新" && git push
```
等 30–60 秒網址就會是新版（頁面最下面「設定」有版本號可對）。

## 改後台程式（Code.gs）
改完要到 Apps Script：貼上 → 儲存 → 部署 → 管理部署作業 → ✎ → 版本選「建立新版本」→ 部署。網址不會變。
坑：試算表會把 2026-09-11 自動變日期，Code.gs 已用 txtDay()/setNumberFormat('@') 處理，不要拿掉。
坑：Google 的回應偶爾會變成一頁 HTML（約 1/4 機率），前端 fetchJSON() 會自動重試 3 次，不要拿掉。
欄位新增：R_HEAD 只能往最後面加（例 film 在 updatedAt 之後），sheetOf 會自動補標題列。

## 本機測試（不碰真後台）
`mock_gas.py`（在 AI 的 scratchpad，需要時重寫）模擬後台；開 App 時網址加 `?api=http://127.0.0.1:8767` 就會連假的。
