# IP 週報 App 使用說明

**網址**：https://harry13428.github.io/ip-report/
（跟網感訓練 App 完全分開，不同 repo、不同網址、不同資料）

## 這是什麼
剪輯師每週五回報「自己負責的 IP 這週的片況」的手機網頁 App。
每個人填自己的，所有人都看得到彼此的，主管打開「全部人」就是整週彙整。

## 給同事的三步（每台裝置只做一次）
1. 用手機 Safari／Chrome 開網址，先按「設定」
2. 填名字（之後不要改）→ 貼上 KK 給的鑰匙 → 按「測試連線並儲存設定」
3. 看到「連線成功」就完成。iPhone 可按分享 → 加入主畫面，當 App 用

## 每週怎麼填
- 「我的回報」→ 先「＋新增 IP」把自己負責的 IP 加進來（只要加一次）
- 點 IP 名字展開 → 選狀態（順利／等回覆／卡住／已過片）→ 填 7 個欄位 → 按「儲存並上傳」
- 沒上傳的會標「未上傳」；按「還原成上次上傳」可以退回
- 上面的 ‹ › 可以切週，週六、週日算下一週的回報
- 「複製我這週的回報文字」→ 直接貼到 LINE 群組接龍

## 給主管
- 「全部人」分頁＝所有人這週的回報，紅字＝需要主管協助
- 上面可以篩「卡住」「需要主管協助」「等回覆」
- 「複製全部人這週的回報文字」→ 整週彙整一次貼走

## 鑰匙怎麼拿（KK 做一次，整個團隊共用）
1. 電腦開 https://github.com/settings/personal-access-tokens/new（登入 harry13428）
2. Token name 打「IP週報」；Expiration 選最長
3. Repository access 選「Only select repositories」→ 只勾 `ip-report-data`
4. Permissions → Repository permissions → **Contents** 改 **Read and write**
5. 最下面 Generate token → 複製整串 `github_pat_…`，自己貼進 App，也用 LINE 傳給同事

鑰匙只能碰 `ip-report-data` 這個私人資料倉庫，碰不到其他東西。鑰匙到期或外流就到同一個頁面刪掉重做一把，大家重新貼一次。

## 資料放哪
- App 本體：GitHub repo `harry13428/ip-report`（公開，只有網頁檔）
- 回報資料：GitHub repo `harry13428/ip-report-data`（私人）→ `editors/名字.json` 一人一檔
- 每次上傳都是一個版本，改壞了可以在 GitHub 的歷史紀錄找回

## 更新 App（給 AI 或 KK）
正本在 `桌面/IP週報App/index.html`。改完：
```bash
cd ~/Desktop/IP週報App && git add index.html && git commit -m "更新" && git push
```
等 30 秒左右網址就會是新版（頁面最下面「設定」有版本號可對）。

## 本機測試（不碰真鑰匙）
`mock_gh.py` 是假的 GitHub API，開 App 時網址加 `?api=http://127.0.0.1:8766` 就會連假的。
